pandas>=2.0
polars>=1.0
pyarrow>=15.0        # parquet I/O
numpy>=1.24
matplotlib>=3.7
jupyter>=1.0
nbformat>=5.9
nbconvert>=7.0
pytest>=7.0        # test suite
scikit-learn>=1.3  # modeling: classifiers, calibration, Poisson GLM
xgboost>=2.0       # modeling: boosting (1X2 + Poisson goals)
catboost>=1.2      # modeling: boosting (1X2 + Poisson goals)
optuna>=3.0        # modeling: temporal hyperparameter search
# tabpfn>=2.0      # optional foundation model — needs TABPFN_TOKEN + GPU
